using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class YoSwag : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("swag to the yo");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
